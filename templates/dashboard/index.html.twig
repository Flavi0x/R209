{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
<div class="container mt-5 text-center">
    <h1 class="display-4 mb-4">Bienvenue sur mes notes</h1>
    
    <p class="lead mb-5">
        Utilise la navigation ci-dessous pour accéder rapidement aux différentes sections.
    </p>

    <div class="row justify-content-center">
        <div class="col-md-3 mb-3">
            <a href="{{ path('app_note_index') }}" class="btn btn-primary btn-lg w-100">
                📓 Notes
            </a>
        </div>
        <div class="col-md-3 mb-3">
            <a href="{{ path('app_tag_index') }}" class="btn btn-success btn-lg w-100">
                🏷️ Tags
            </a>
        </div>
        <div class="col-md-3 mb-3">
            <a href="{{ path('app_etat_index') }}" class="btn btn-warning btn-lg w-100">
                📊 États
            </a>
        </div>
    </div>

    <hr class="my-5">

    
    
</div>
{% endblock %}
